# Gitleaks configuration for dotfiles/shell_config repo
# Scans shell configs, git configs, and related dotfiles for secrets.

title = "gitleaks config for dotfiles"

[allowlist]
  description = "Verified false positives"
  paths = [
    '''gitleaks-report\.json''',
    '''\.secrets\.baseline''',
  ]
